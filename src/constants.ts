export const enum LoadingStatus {
  NONE,
  LOADING,
  READY,
  ERROR,
}
export const enum UserRole {
  NO_REGISTRATION,
  USER,
  ADMIN,
}

export const enum UserCategory {
  BLOGGER,
  COMMUNITY,
  ORGANIZATION,
  EDITOR,
  WRITER,
  GARDENER,
  FLOWER_MAN,
  PHOTOGRAPHER,
}

export const enum UserGender {
  MALE,
  FEMALE,
  OTHER,
}
export const enum UserMood {
  IRRITATION,
  ANXIETY,
  CONCENTRATION,
  HAPPINESS,
}

export const enum AuthenticationStatus {
  NONE,
  NO_AUTHORIZED,
  AUTHORIZED,
}
